<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Seedling Scheduler" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Seedling Scheduler">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# PWA manifest disabled - uncomment below to enable Progressive Web App features %>
    <%# tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Vite manages all CSS and JS assets %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application', "data-turbo-track": "reload" %>
    <!--
      If using a TypeScript entrypoint file:
        vite_typescript_tag 'application'

      If using a .jsx or .tsx entrypoint, add the extension:
        vite_javascript_tag 'application.jsx'

      Visit the guide for more information: https://vite-ruby.netlify.app/guide/rails
    -->

  </head>

  <body>
    <%# Bootstrap Navigation Bar %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container-fluid">
        <%= link_to "Seedling Scheduler", root_path, class: "navbar-brand" %>

        <%# Mobile hamburger toggle %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <%# Navigation links %>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Getting Started", getting_started_path, class: "nav-link #{current_page?(getting_started_path) ? 'active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Tasks", tasks_path, class: "nav-link #{(current_page?(tasks_path) || current_page?(calendar_tasks_path)) ? 'active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Plants", plants_path, class: "nav-link #{current_page?(plants_path) ? 'active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Journal", garden_entries_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Settings", edit_settings_path, class: "nav-link #{current_page?(edit_settings_path) ? 'active' : ''}" %>
            </li>
          </ul>

          <%# Authentication links on the right %>
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <span class="navbar-text text-white me-3">
                  <%= current_user.email %>
                </span>
              </li>
              <li class="nav-item">
                <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Log In", new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <%# Main Content Container %>
    <div class="container mt-4">
      <%# Flash messages and notification container %>
      <div data-controller="notification" data-notification-target="container">
        <% if flash.any? %>
          <% flash.each do |type, message| %>
            <div class="alert alert-<%= type == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
              <%= message %>
              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
          <% end %>
        <% end %>
      </div>

      <%= yield %>
    </div>

    <%# Footer %>
    <footer class="mt-5 py-4 bg-light">
      <div class="container text-center text-muted">
        <small>Seedling Scheduler &copy; <%= Time.current.year %></small>
      </div>
    </footer>
  </body>
</html>
